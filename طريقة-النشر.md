# طريقة نشر المنصة على Vercel

المشروع جاهز بالكامل ومُختبر. تبقّى تشغيل أمرين على جهازك.

> **قبل البدء:** احذف مجلد `node_modules` الموجود حالياً (تركيبه غير مكتمل).
> اضغط عليه بزر الفأرة الأيمن ثم Delete.

---

## الخيار الأول — النشر المباشر (الأسرع · دقيقتان)

افتح **PowerShell** داخل مجلد المشروع، ثم:

```powershell
npm install
npx vercel --prod
```

- عند أول تشغيل سيطلب `vercel` تسجيل الدخول: اختر **Continue with GitHub**
  وسيفتح المتصفح لتأكيد الدخول بحسابك.
- ثم يسأل أسئلة قصيرة — اقبل الإجابات الافتراضية بالضغط على Enter،
  واختر الفريق **dga-team** عند السؤال عن الـ scope.
- في نهاية التنفيذ سيطبع رابط الإنتاج.

---

## الخيار الثاني — عبر GitHub مع نشر تلقائي عند كل تحديث (موصى به)

1. أنشئ مستودعاً فارغاً على GitHub باسم:
   **dga-strategy-execution-platform** (بدون README ولا .gitignore).

2. في PowerShell داخل مجلد المشروع:

```powershell
git init
git add .
git commit -m "منصة رصد تنفيذ استراتيجية التحول الرقمي"
git branch -M main
git remote add origin https://github.com/abinjadid/dga-strategy-execution-platform.git
git push -u origin main
```

3. أخبرني أن المستودع جاهز — سأربطه بـ Vercel وأطلق النشر،
   وبعدها يُنشر أي تحديث تلقائياً عند كل `git push`.

---

## تشغيل المنصة محلياً للتجربة

```powershell
npm install
npm run dev
```

ثم افتح: http://localhost:3000

---

## ملاحظات

- **الخط:** ملفات Diodrum Arabic موجودة في `public/fonts/` وتُستخدم تلقائياً.
  أضيف خط `IBM Plex Sans Arabic` كاحتياطي فقط في حال تعذّر تحميلها.
- **التخزين:** بيانات المنصة تُحفظ في متصفح كل مستخدم (localStorage).
  للترقية إلى قاعدة بيانات مشتركة يُستبدل ملف `src/lib/store.ts` وحده
  بمناداة API مع الحفاظ على نفس أسماء الإجراءات — دون تعديل أي شاشة.
- **إعادة التعيين:** من شاشة «إدارة المنصة» → تبويب «استيراد وتصدير».
