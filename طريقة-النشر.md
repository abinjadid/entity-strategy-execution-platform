# نظام إدارة استراتيجية التحول الرقمي — منشور ✅

## الروابط

| | |
|---|---|
| **الرابط المباشر** | https://dga-strategy-platform.vercel.app |
| **رابط بديل (بالاسم الجديد)** | https://entity-digitalstrategy-platform-dga-team.vercel.app |
| **المستودع** | https://github.com/abinjadid/entity-strategy-execution-platform |
| **لوحة Vercel** | https://vercel.com/dga-team/entity-digitalstrategy-platform |

الفرع الإنتاجي: `main` — أي `git push` إليه يُنشر تلقائياً خلال دقيقة.

### للحصول على رابط نظيف بالاسم الجديد

عند إعادة تسمية المشروع أبقت Vercel النطاق القصير القديم كما هو. لإضافة الجديد:

1. افتح: https://vercel.com/dga-team/entity-digitalstrategy-platform/settings/domains
2. اضغط **Add Domain** واكتب: `entity-digitalstrategy-platform.vercel.app`
3. **Add** — ثم اجعله Primary إن رغبت

---

## تحديث المنصة لاحقاً

```powershell
git clone https://github.com/abinjadid/entity-strategy-execution-platform.git
cd entity-strategy-execution-platform
npm install
```

بعد أي تعديل:

```powershell
git add .
git commit -m "وصف التعديل"
git push
```

> **تنبيه:** مجلدا `.git` و`node_modules` الموجودان في هذا المجلد معطوبان
> (نشآ عن محاولة داخل مجلد لا يسمح بحذف الملفات). احذفهما واعمل على النسخة
> المستنسخة أعلاه — هي المرتبطة بالنشر التلقائي.

---

## التشغيل محلياً

```powershell
npm install
npm run dev
```

ثم افتح: http://localhost:3000

---

## أدوار الدخول

| الدور | الصلاحية |
|---|---|
| مدير النظام | كل شيء: الهيكل، المستخدمون، الإعدادات، الاستيراد والتصدير |
| مدير متابعة الاستراتيجية | متابعة الكل وتحديث أي مشروع أو مؤشر |
| مالك مبادرة / مشروع | تحديث مبادراته ومشاريعه فقط |
| مشاهد التقارير | اطلاع وتصدير دون تعديل |

---

## ملاحظات فنية

- **التخزين:** البيانات تُحفظ في متصفح كل مستخدم (localStorage). للترقية إلى
  قاعدة بيانات مشتركة يُستبدل ملف `src/lib/store.ts` وحده بمناداة API مع
  الحفاظ على نفس أسماء الإجراءات — دون تعديل أي شاشة.
- **الخط:** Diodrum Arabic في `public/fonts/` ويُحمَّل من الموقع مباشرة.
- **إعادة تعيين البيانات:** من شاشة «إدارة المنصة» ← تبويب «استيراد وتصدير».
- **صلاحية GitHub:** مُنحت لتطبيق «GitHub CLI»؛ يمكن سحبها من
  github.com/settings/applications
